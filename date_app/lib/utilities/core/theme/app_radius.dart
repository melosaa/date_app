import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppRadius {
  static BorderRadius small = BorderRadius.all(Radius.circular(8.r));
  static BorderRadius medium = BorderRadius.all(Radius.circular(12.r));
  static BorderRadius gender = BorderRadius.all(Radius.circular(10.r));
  static BorderRadius large = BorderRadius.all(Radius.circular(16.r));
  static BorderRadius extraLarge = BorderRadius.all(Radius.circular(24.r));
}
